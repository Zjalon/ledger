# Vite（复制为 `.env` 使用；前缀 VITE_）
VITE_GTAG_SCRIPT=
VITE_LOGIN_API_HOST="http://localhost"
VITE_RATE_API_HOST="http://localhost"

# README 截图脚本：复制本文件中下列两行到 `.env.local`（已被 git 忽略），填写令牌。
# 勿将填好令牌的 `.env.local` 提交到仓库。
# GITEE_TOKEN=
# BASE_URL=http://127.0.0.1:5173
